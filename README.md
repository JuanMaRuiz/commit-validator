# Git commit Validator

> This is a work in progress

## Configuration
```
{
  "git-commit-validator": {
    "limits": {
      "subject": "40",
      "description": "80"
    },
    "subjectPattern": "^(clos|fix|resolv)(e[sd]|ing)",
  }
}
```